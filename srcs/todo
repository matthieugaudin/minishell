



-> have a working exec :
    -> testing : pipex tester tests : 
-> display a prompt
-> handle echo + not a posix after (if not a posix but not $?) :
-> expand the $? exit code: 
-> handle $VAR and "$VAR"
-> check_builtins in execve (fork ? dup ?) : 
-> handle singals
-> free data + close fds + settle valgrind errors
-> pass subject tests
-> pass testers
-> protect every syscall
-> add comments to each function for team mate


TESTS : 
- no command
- ctrl on here_doc
- enter with nothing
- enter with spaces