


-> redirect_fds (verify not the first)
-> check_builtins
-> update makefile
-> refactor properly all files and headers as an API
-> display a prompt
-> handle echo + not a posix after (if not a posix but not $?) :
-> expand the $? : 
-> handle $VAR and "$VAR"
-> handle singals
-> no free on each line
-> protect every syscall


TESTS : 
- no command
- ctrl on here_doc